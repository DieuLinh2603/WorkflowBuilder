package com.company.workflowbuilder.entity.workflow;

public enum ConnectionType {
    DEFAULT, IF, ELSE, APPROVE, REJECT, REVIEW_PASS, REVIEW_FAIL,
    ASSIGNMENT_DONE, ASSIGNMENT_FAIL, SYSTEM_SUCCESS, SYSTEM_FAIL
}
